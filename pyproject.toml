[tool.ruff]
line-length = 88
extend-select = ["E", "F", "W", "C", "B"]

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
check_untyped_defs = true

[tool.bandit]
# Minimal bandit config placeholder
skips = []
